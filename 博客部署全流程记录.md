# 博客部署全流程记录

> 记录时间：2026-03-23  
> 作者：苗泽盛（Erwang）

---

## 一、整体架构

```
用户访问 erwang.top
    ↓
Cloudflare（HTTPS + CDN 加速）
    ↓
阿里云服务器 47.102.109.115（上海）
    ↓
Nginx 返回 /www/wwwroot/erwang/index.html
    ↓
用户看到页面
```

---

## 二、已有资源

| 资源 | 详情 |
|------|------|
| 域名 | `erwang.top`，在 NameSilo 购买，Cloudflare 管理 DNS |
| 服务器 | 阿里云轻量应用服务器，上海，2核2G，IP：`47.102.109.115` |
| 操作系统 | Alibaba Linux 3 |
| 面板 | 宝塔面板 v11，访问地址：`http://47.102.109.115:8888` |
| 代码托管 | GitHub：`github.com/miaozesheng/erwang` |

---

## 三、服务器配置

### 3.1 SSH 连接服务器

```bash
ssh root@47.102.109.115
```

### 3.2 宝塔面板

访问地址：`http://47.102.109.115:8888/login`

初始账号信息通过以下命令获取：
```bash
bt default
```

### 3.3 已安装软件

- Nginx 1.28
- MySQL 5.7
- JDK 1.8

---

## 四、网站部署

### 4.1 拉取代码

```bash
# 安装 git
dnf install git -y

# 拉取代码
cd /www/wwwroot
git clone https://github.com/miaozesheng/erwang.git
```

### 4.2 宝塔创建网站

1. 宝塔面板 → 网站 → 添加站点
2. 域名：`erwang.top`
3. 根目录：`/www/wwwroot/erwang`
4. PHP：静态
5. 点提交

### 4.3 更新网站内容

本地修改代码后 push 到 GitHub，然后：

```bash
cd /www/wwwroot/erwang && git pull
```

---

## 五、Cloudflare 配置

### 5.1 DNS 记录

| 类型 | 名称 | 内容 | 代理 |
|------|------|------|------|
| A | `@` | `47.102.109.115` | 橙色云（开启） |
| A | `www` | `47.102.109.115` | 橙色云（开启） |

### 5.2 SSL 模式

SSL/TLS → 加密模式：**Full**

### 5.3 直连白名单（Ghelper）

在 Ghelper 上网策略中添加直连：
```
47.102.109.115
172.24.41.177
```

---

## 六、SSL 证书

### 申请方式

宝塔面板 → 网站 → 设置 → SSL → Let's Encrypt → DNS验证

需要配置 Cloudflare DNS 接口：
- 验证类型：Cloudflare
- ID：`2408757673@qq.com`
- Token：Cloudflare Global API Key

### 获取 Global API Key

Cloudflare → My Profile → API Tokens → Global API Key → View

---

## 七、当前问题与解决方案

### 问题：国内服务器需要 ICP 备案

**原因：** 阿里云国内服务器访问 80/443 端口需要 ICP 备案，域名注册商 NameSilo 未获工信部批复，无法直接备案。

**临时方案：切回 GitHub Pages 托管**

将 Cloudflare DNS 两条 A 记录内容改为：
```
185.199.108.153
```
同时确认 GitHub Pages 设置里自定义域名还是 `www.erwang.top`。

**备案通过后：切回阿里云服务器**

将 Cloudflare DNS 两条 A 记录内容改回：
```
47.102.109.115
```

两套方案随时可切换，改 DNS 后等 1-2 分钟生效。

**长期方案：** 等域名购买满 60 天后，转移到阿里云注册商，再申请 ICP 备案（约需 1-2 周）。

备案通过后：
- 80/443 端口解除限制
- SSL 证书可正常申请
- 网站可正常通过域名访问

---

## 八、常用命令速查

```bash
# SSH 连接服务器
ssh root@47.102.109.115

# 更新网站代码
cd /www/wwwroot/erwang && git pull

# 重启 Nginx
systemctl restart nginx

# 查看 Nginx 状态
systemctl status nginx

# 查看宝塔面板信息
bt default
```

---

## 九、后续计划

- [ ] 等域名满 60 天，转移到阿里云注册商
- [ ] 申请 ICP 备案
- [ ] 备案通过后申请 SSL 证书
- [ ] 配置 GitHub Actions 自动部署
- [ ] 完善博客内容

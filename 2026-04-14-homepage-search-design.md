# 首页搜索区改版设计文档

## 背景

当前首页搜索区位于 `frontend/src/views/ArticleList.vue`，由搜索输入框、分类下拉、日期范围和检索按钮组成，同时页面左侧还存在一套分类 / 标签 pill 筛选。实际体验存在以下问题：

- 同一个“分类”同时存在 sidebar pill 和下拉框两套入口，逻辑重复，增加认知负担。
- 搜索区整体更像后台筛选表单，而不是面向读者的内容发现入口。
- 日期筛选默认暴露且权重过高，不符合博客读者的常用行为。
- 桌面端结构割裂，移动端只是换行而不是重新组织层级。

本次改版目标不是“美化表单”，而是把首页顶部检索区从后台式工具条，重构为杂志型内容产品的阅读入口。

## 设计上下文

依据 `.impeccable.md`：

- 用户：技术同行、技术阅读者、具备较强内容检索与阅读能力的 C 端用户。
- 核心任务：高效发现文章、快速理解当前内容范围、顺畅开始阅读。
- 品牌气质：personal / credible / refined。
- 产品方向：light-brand personal blog，避免 dashboard 感，强调阅读优先。

## 设计结论

采用 **“编辑部检索条”** 方案。

整体策略：

- 强化“搜索”作为主入口。
- 将“分类”转化为内容栏目式胶囊导航。
- 将“日期”降级为高级筛选，默认几乎隐藏。
- 用状态条展示当前筛选上下文，而不是把复杂控件常驻暴露给用户。

## 结构设计

搜索区拆分为三层：

### 1. 主搜索框

首页搜索区的第一视觉焦点。

- 宽度足够大，承担主要输入入口。
- placeholder 使用更自然的内容产品语言：`搜索你想读的内容`
- 支持回车搜索、清空、focus 状态强化。

### 2. 分类胶囊带

替代原有“分类下拉框 + 侧边重复分类”的冗余结构。

- 采用栏目式横向 chips：`全部 / 前端 / 后端 / AI / 随笔 ...`
- 点击即触发筛选，不需要再点“应用”或“确认”
- 移动端支持横向滑动
- 保留 active 状态、hover 状态与切换动画

### 3. 状态条

用于解释“系统当前如何在筛选结果”。

示例：

- `前端 · 关键词 “vite” · 找到 12 篇文章`
- `全部文章 · 找到 48 篇文章`

状态条承担三个职责：

- 呈现当前关键词 / 分类 / 日期条件
- 呈现结果数
- 提供 `清除条件` 与 `更多筛选`

## 交互设计

### 基础交互

- 用户输入关键词后，按回车或点击搜索按钮触发搜索。
- 用户点击分类胶囊后，立即刷新列表结果。
- 用户已输入关键词后切换分类，结果按组合条件刷新。
- 只有条件生效时，才显示清除入口。

### 高级筛选

日期筛选默认不常驻。

- 通过 `更多筛选` 或 `按时间筛选` 展开
- 日期筛选仅在用户主动需要时出现
- 日期生效后写入状态条；未生效时不显示在主界面

建议加入快捷时间项：

- 近 30 天
- 近半年
- 自定义范围

### 状态反馈

从“控件状态”转为“阅读上下文状态”：

- 默认：只显示搜索框 + 分类胶囊
- 有关键词：状态条出现并说明当前检索词
- 有分类：分类 chip 高亮并同步到状态条
- 有日期：日期只在生效时显示到状态条
- 有任一条件：显示 `清除条件`

## 文案策略

文案语气采用 **更克制、更高级的内容产品表达**，避免两种极端：

- 避免后台式指令文案，例如“检索”“开始/结束”“提交筛选”
- 避免过度拟人、过度热情、过度自我表达的作者口吻

整体原则：

- 词少，但信息明确
- 优先描述内容，而不是描述控件
- 让用户感觉自己在浏览一个成熟的内容产品，而不是在操作一个后台系统

推荐文案：

- 搜索框：`搜索文章`
- 日期入口：`时间`
- 清除按钮：`清除`
- 更多筛选：`更多筛选`
- 状态条示例：`前端 · vite · 12 篇文章`
- 空状态标题：`没有找到相关文章`
- 空状态说明：`试试其他关键词，或清除当前筛选条件`
- 空状态操作：`清除筛选` / `查看全部文章`

不推荐文案：

- `搜索你想读的内容`（偏直白，略显用力）
- `按时间筛选`（说明味太重）
- `没有找到完全匹配的文章`（语气偏重，不够克制）

目标不是“说得更热情”，而是“说得更准、更轻、更像编辑部而不是后台”。

## 视觉设计方向

视觉上采用“独立博客 / 编辑部栏目导航”思路，而不是控制台筛选条：

- 搜索框更像内容入口，而不是表单控件拼接。
- 分类胶囊更像栏目导航，而不是标签管理器。
- 状态条更像轻量编辑注释，而不是工具栏。
- 通过留白、节奏、边界强弱关系建立层级。

不采用的方向：

- 不做后台查询表单风格
- 不做重型 dashboard 工具条
- 不做默认显眼的大面积日期选择器

## 动效与细节

动效应克制、安静、服务于状态变化。

- 分类 chip 切换：轻微位移 + 底色过渡
- 搜索框 focus：边界与阴影轻微增强
- 高级筛选展开：短促 reveal，不做强侵入动画
- 状态条出现 / 消失：淡入 + 微小位移

遵循 reduced motion 兼容，避免动画喧宾夺主。

## 响应式设计

### 桌面端

- 第一行：主搜索框
- 第二行：分类胶囊带
- 第三行：状态条 + 更多筛选 / 清除条件

### 移动端

- 搜索框保持优先显示
- 分类胶囊改为横向滑动
- 高级筛选改为折叠区或轻量展开区
- 禁止把搜索、分类、日期硬挤在同一行

## 数据与逻辑调整建议

### 需要消除的重复入口

当前 `selectedCategory` 同时被 sidebar pill 与搜索栏下拉控制。改版后应只保留一套主要分类交互。

建议：

- 首页主内容区保留分类胶囊导航
- 删除搜索条中的分类下拉
- 评估是否需要弱化或重构左侧 sidebar 分类逻辑，避免一页两套分类入口继续并存

### 查询模型

保留现有查询参数能力：

- `keyword`
- `category`
- `startDate`
- `endDate`

但在界面层面重构其暴露方式，确保主界面优先服务“搜索 + 分类”，日期作为次级条件存在。

## 实施范围

主要涉及：

- `frontend/src/views/ArticleList.vue`
- 可能涉及全局样式 token 的复用与抽取
- 可能涉及 sidebar 筛选结构的职责调整

## 验收标准

### 用户体验

- 首页搜索区第一眼可理解，无后台表单感
- 用户能快速完成“搜关键词”与“按分类看文章”两个高频动作
- 日期筛选不再干扰默认浏览体验
- 用户能清楚看到当前筛选条件是否生效

### 视觉与交互

- 搜索区具备更强层级与更轻的阅读入口感
- 分类交互具备明确选中反馈
- 状态条能够解释结果来源
- 移动端不再出现拥挤、表单堆叠、难以浏览的情况

### 实现约束

- 延续现有 Vue 3 + Element Plus 技术栈
- 保持现有查询参数与接口兼容
- 避免引入与品牌气质冲突的重装饰风格

## 方案摘要

本次方案的本质不是给旧表单“换皮”，而是把首页搜索区重新定义为：

**一个帮助读者发现内容、理解上下文、快速进入阅读的杂志型入口。**
